# 도스트11 카드 단순화 · 전후 가로 배치 설계

2026-06-05, 비주얼 컴패니언으로 검증·승인됨 (선택: 그룹 해체).

## 배경

CG 도구·most267·포토부스가 '업무' 태그의 독립 타임라인 항목으로 분리된 뒤에도
도스트11 카드에 01/02/03 그룹 요약이 남아 내용이 중복됐다. 또한 업무 항목의
전후 미디어가 우측 칼럼 세로 스택이라 항목이 길어져 한 화면에 들어오지 않았다.

## 결정

1. **카드 그룹 해체** — 도스트11 카드의 contributions를 그룹 없는 평문 불릿
   3개(도프켓 요점)로 축소. CG·most267 요약은 카드에서 삭제 (독립 항목과 중복).
2. **전후 가로 한 줄** — beforeAfter를 가진 항목은 미디어를 우측 칼럼이 아니라
   텍스트 아래 풀폭에 배치, 전·후를 가로로 나란히 (모바일에서는 세로 폴백).
   목적: 전후가 항상 같은 화면에 함께 보이고 항목 높이가 줄어든다.

## 구현 범위

- `ko.json`: 도스트11 contributions → 문자열 불릿 3개
- `ProjectEntry.tsx`: beforeAfter 항목은 article을 세로 흐름으로 두고
  figure를 w-full로, BeforeAfterStack을 `flex-col sm:flex-row`로 변경
- 기존 카드 렌더(그룹 헤더·테크 칩)는 그룹이 없으면 자연히 미표시 — 코드 변경 없음
